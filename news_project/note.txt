Design
 각 카테고리별로 맞는 뉴스 보여주기

기본 경로에는 전체적인 뉴스를 보여준다

그리고 각 카테고리에 들어가면 그에 맞는 뉴스를 보여준다.

그리고 context api 사용하기



1. 레이아웃 페이지와 함께 링크 만들기

2. 기본 경로에는 모든 뉴스를 보여준다.

3. 각 카테고리별로 맞는 뉴스를 보여준다.

4. NavLink 활성화   clear


5. NewsList에서 mapping할 떄  key값으로 고유한 값 주기      clear

url을 key값으로 줌.

6. 컨텐츠 정리


---Error

1 .
다른 카테고리를 들어가려할 때 불러올 데이터가 바로 조회가 안됨.
useEffect에서 처음 렌더링 될 때만 fetchData가 되게 했다.
data가 바뀔 때마다 fetchData가 되도록 하자.

=> param을 dependency로 설정해줌.



2.



----수정사항----
1. 이미지 크기가 크다.
- 이미지 크기 줄이기

2. css적인 부분
styled-component를 사용해야할까?
styling이 많지 않아서 굳이 사용할 필요없을 듯하다.

3. js적인 부분
- NewsItem부분에서 건들부분 없음.

- data가 변경되는 부분은 NewList이기 때문에 굳이 App 컴포넌트에서 state 설정할 필요없음.

- 왜 두번 리렌더링 될까
NewList가 렌더링될 때 state값은 처음에 null이다.
렌더링되면서 fetchData가 호출되면서 리렌더링된다.
그렇게 두번 렌더링이 된다.

그런데 다른 카테고리를 누르면 이전 category value가 렌더링되고 다음 category value가 렌더링된다. 

> 삼항연산식을 쓰면 리렌더링이 두 번 된다? - x

> &&연산자 때문에 리렌더링이 두 번 된다? - x

> url/params가 바뀌면 컴포넌트가 리렌더링된다.
이 이유 때문에 리렌더링이 2번 되는 것이었다.
우선 params가 바뀌면 컴포넌트가 리렌더링된다.

params가 바뀌었을 때 첫번째 리렌더링에 value값은 변하지 않는다.
이유는 렌더링한뒤에 useEffect가 실행되기 때문이다.

따라서 첫번째 리렌더링이 된 이후에 useEffect가 실행되고 거기서 value값을 다시 설정해주기때문에
두번째 리렌더링이 실행되는 것이다.


(하지만 같은 카테고리를 다시한번 눌렀을 때는 리렌더링이 한번만 된다.
dependency값이 바뀌지 않으니 useEffect가 실행되지 않는것)


- api를 비동기적으로 받아와야하는 이유


5. nomatch 구현
params때문에 해당 params이 없어도 no match부분이 렌더링이 안됨.

Error
> Indicate whether to send a cookie in a cross-site request by specifying its SameSite attribute



useEffect는 컴포넌트 렌더링된 뒤에 실행된다.