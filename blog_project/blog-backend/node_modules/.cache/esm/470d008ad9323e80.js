let Koa,Router,bodyParser,mongoose,api;_d93‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_d93‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_d93‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_d93‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d93‍.w("./api/index",[["default",["api"],function(v){api=v}]]);require('dotenv').config();






const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MonogoDB');
  })
  .catch((e) => {
    _d93‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes()); // nest routing

// 라우터 적용 전에 bodyparser 적용
app.use(bodyParser());

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  console.log(`Listening to port ${port}`);
});
