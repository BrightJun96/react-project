let mongoose;_081‍.x([["default",()=>_081‍.o]]);_081‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String],
  date: {
    type: Date,
    default: Date.now,
  },
  user: {
    _id: mongoose.Types.ObjectId,
    username: String,
  },
  like: Number,
});

const Post = mongoose.model("Post", PostSchema);

_081‍.d(Post);
